<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.partial.ejs', {title: 'Data'}) %>
</head>

<body>
    
    <% for (var i=0; i < data.length;  i++ ) { %>
        <% for (const [key, value] of Object.entries(data[i])) { %>
            <%= key %> : <%= value %>
            <br />
        <% } %>
        <br />
    <% } %>

</body>

</html>